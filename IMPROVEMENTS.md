cards are never removed from the table
the game reveals both dealer cards instantly instead of keeping one hidden
the game does not handle the aces as soft/hard values which is important for accurate scoring
unnecessary shuffling after every hit.
# 
Fix first:
shuffle at the start of a new game rather than after every hit.
Real 52 card deck (removes duplicate card issue)
Ace handling (1 or 11)

Clean up:
PEP 8 naming — snake_case for variables and methods
Remove the print() debug statements

README:
Add a screenshot of the GUI
Add how to run it instructions


